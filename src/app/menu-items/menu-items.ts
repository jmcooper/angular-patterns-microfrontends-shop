import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'bot-menu-items',
  imports: [RouterModule],
  templateUrl: './menu-items.html',
  styleUrl: './menu-items.css'
})
export class MenuItems {

}
